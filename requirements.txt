# Starship Safety — judging environment (pinned).
#
# Install on the judge (VPS):
#     .venv/bin/pip install -r requirements.txt
#
# IMPORTANT: the ML versions below are also the environment PARTICIPANTS must
# build against. A joblib model saved with, say, scikit-learn 1.9.0 must be
# loaded by the same 1.9.0 on the judge — a mismatch causes MODEL_FAILED.
# Publish the "ML / data" block to participants as the official environment.

# --- Web service: portal + worker (judge only; not needed by participants) ---
fastapi==0.139.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
requests==2.32.5

# --- ML / data: the allowed libraries (must match config.ALLOWED_LIBRARIES) ---
numpy==2.4.0
pandas==2.3.3
scipy==1.18.0
scikit-learn==1.9.0
joblib==1.5.3
xgboost==2.1.3
lightgbm==4.5.0
